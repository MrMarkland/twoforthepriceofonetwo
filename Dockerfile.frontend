FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json* ./
RUN npm install

# Copy the rest of the app
COPY . .

# Build Next.js
RUN npm run build

ENV PORT=8080
EXPOSE 8080

# Cloud Run: start the production server
CMD ["npm", "start"]
